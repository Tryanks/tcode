[package]
name = "tcode-core"
version = "0.1.0"
edition = "2024"

[dependencies]
agent = { path = "../agent" }
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
