[package]
name = "tcode-remote"
version = "0.1.0"
edition = "2024"

[dependencies]
tcode-protocol = { path = "../protocol" }
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
