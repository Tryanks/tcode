[package]
name = "term"
version = "0.1.0"
edition = "2024"

[dependencies]
async-channel = "2"
rio-graphics = "0.5.24"
rio-vt = { version = "0.5.24", features = ["graphics"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
portable-pty = "0.9"
