[package]
name = "tcode-client"
version = "0.1.0"
edition = "2024"

[dependencies]
tcode-protocol = { path = "../protocol", default-features = false }
async-channel = "2"
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"

[dev-dependencies]
smol = "2"
